/**
 * Utility function to check if a name should be highlighted
 * @param name - The name to check
 * @returns true if the name matches the highlight criteria
 */
export const highlightMe = (name: string): boolean => {
  return name === 'Кисельов І. О.';
};
